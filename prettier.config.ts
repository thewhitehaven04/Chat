import type { Config } from 'prettier'

const config: Config = {
    trailingComma: 'none',
    printWidth: 100,
    tabWidth: 4,
    useTabs: false,
    semi: false,
    singleQuote: true
}

export default config
